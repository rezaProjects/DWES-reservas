<?php
    // Database configuration information
    // This code just defines de constant DBHOST, DBUSER, DBPASS and DBNAME which are needed to connect to the database. 
    // In this way when we need to change the connection parameters , it is enough to change this informations...
    define('DBHOST','mardiadb');
    define('DBUSER','root');
    define('DBPASS','bitnami');
    define('DBNAME','reservas');

